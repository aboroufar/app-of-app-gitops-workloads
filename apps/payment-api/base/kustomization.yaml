apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - db-migration-job.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: app-image
    newName: nginxinc/nginx-unprivileged
    newTag: 1.27-alpine
